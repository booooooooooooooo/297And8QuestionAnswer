class Config:
    def __init__(self, batch_size, embed_s, num_units, n_epoch):
        self.batch_size = batch_size
        self.embed_s = embed_s
        self.num_units = num_units
        self.n_epoch = n_epoch
